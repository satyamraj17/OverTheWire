# Level Goal

There is a git repository at ssh://bandit30-git@bandit.labs.overthewire.org/home/bandit30-git/repo via the port 2220. The password for the user bandit30-git is the same as for the user bandit30.

From your local machine (not the OverTheWire machine!), clone the repository and find the password for the next level. This needs git installed locally on your machine.

# Solution

Similar thing again, clone it and look for the password.
```
┌──(kali㉿kali)-[~/Desktop/Bandit/bandit30]
└─$ cat README.md 
just an epmty file... muahaha
```
It only has one commit.
```
└─$ git log --all
commit bd393e0e59a075f92fd84edc0ad8d13f64572de2 (HEAD -> master, origin/master, origin/HEAD)
Author: Ben Dover <noone@overthewire.org>
Date:   Fri Apr 3 15:17:58 2026 +0000

    initial commit of README.md
```
We need to see the tag.
`Git tags are used to mark specific commits in the repository, typically to identify releases or important milestones.`
We can see the tag using the following:
```
┌──(kali㉿kali)-[~/Desktop/Bandit/bandit30/.git]
└─$ git tag         
secret
```

```
└─$ git show secret
fb5S2xb7bRyFmAvQYQGEqsbhVyJqhnDy
```
